import 'package:flutter/material.dart';

class MainUI2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(),
      body: Text('test'),
    );
  }
}
